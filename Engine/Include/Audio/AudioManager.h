#pragma once

namespace Umbra {
    class AudioManager {
    private:
        /* data */
    public:
        AudioManager(/* args */);
        ~AudioManager();

        void Initialize();
        void Hello();
        // Initialise

        // shutdown
        void ShutDown();

        // play sfx
        // play music
        // stop music
    };
} // namespace Umbra
